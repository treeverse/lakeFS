name: hadoopFS
on:
  push:
    paths-ignore:
      - "client/haddopfs/**"
    branches:
      - master
  pull_request:
jobs:
  lakefs-hadoopfs:
    name: Compile
    runs-on: ubuntu-20.04
    steps:
      - name: Check-out code
        uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          java-version: '15'
          distribution: 'adopt'
      - name: Run the Maven verify phase
        working-directory: client/hadoopfs
        run: mvn --batch-mode --update-snapshots verify
