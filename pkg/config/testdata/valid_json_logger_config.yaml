---
logging:
  format: json
  level: DEBUG
  output: "/tmp/lakefs_json_logger_test.log"

metadata:
  db:
    type: badger
    badger:
      path: /tmp

blockstore:
  type: local
  local:
    path: /tmp

gateways:
  s3:
    domain_name: s3.example.com
    region: us-east-1

listen_address: "0.0.0.0:8005"
