---
database:
  type: local

auth:
  encrypt:
    secret_key: "required in config"

blockstore:
  type: s3
  s3:
    credentials:
      access_key_id: my-key-id
      secret_access_key: my-secret-key
